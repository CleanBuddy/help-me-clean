{
  "name": "helpmeclean-mobile",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.7.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cleaner": "cd packages/cleaner-app && npx expo start",
    "company": "cd packages/company-app && npx expo start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "turbo": "^2.4.0"
  }
}
