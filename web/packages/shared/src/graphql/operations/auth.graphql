mutation SignInWithGoogle($idToken: String!, $role: UserRole!) {
  signInWithGoogle(idToken: $idToken, role: $role) {
    token
    user {
      id
      email
      fullName
      role
      status
    }
    isNewUser
  }
}
