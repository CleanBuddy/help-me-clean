type PlatformSetting {
  key: String!
  value: String!
  valueType: String!
  description: String
  updatedAt: DateTime!
}

extend type Query {
  platformSettings: [PlatformSetting!]!
}

extend type Mutation {
  updatePlatformSetting(key: String!, value: String!): PlatformSetting!
}
